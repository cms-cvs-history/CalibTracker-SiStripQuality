process Test = {

    untracked PSet maxEvents = {untracked int32 input = 1}
    
    source = EmptySource {
	untracked uint32 firstRun = 1   
	untracked uint32 numberEventsInRun = 1
    }
    
    service = MessageLogger {
	# put path and name of logfile here 
	# untracked vstring destinations = { "read_badstripfromconstructiondb_noblob.log" }
	# untracked PSet read_badstrip_geom_ideal_noblob.log = { untracked string threshold = "INFO" }

    }
    
    service = Timing {}
    
    
    es_source = PoolDBESSource { 
VPSet toGet = {{ string record = "SiStripBadStripRcd" string tag = "mytest_bs" }}
	
	#put path and name of db and xml files here
	#string connect = "sqlite_file:badstripfromconstructiondb_noblob.db"    
	#untracked string catalog = "file:badstripfromconstructiondb_noblob.xml"

	string timetype = "runnumber" 
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked int32 messageLevel=2
#blob	    untracked bool loadBlobStreamer = true
	}
    }
    
module prod =  SiStripBadStripReader {
untracked bool   printDebug = true
                   }

       path p = { prod }

       module print = AsciiOutputModule {}
       endpath ep = { print }
}
