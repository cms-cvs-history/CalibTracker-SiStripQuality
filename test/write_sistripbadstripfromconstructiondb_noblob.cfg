process Test = {
    
    source = EmptySource {
	untracked uint32 firstRun = 1 
	untracked uint32 numberEventsInRun = 1
	
    }
    
	untracked PSet maxEvents = {untracked int32 input = 1}	

    service = MessageLogger {
	# put name and path of logfile here
	# untracked vstring destinations = { "write_sistripbadstripfromconstructiondb_noblob.log" }
	# untracked PSet write_sistripbadstripfromconstructiondb_noblob.log = { untracked string threshold = "INFO" }
    }
    
    
         #include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
    
    #es_module = TrackerGeometricDetESModule {}
    #es_module = TrackerDigiGeometryESModule {}
    
    service = Timing {}
    
    service = PoolDBOutputService{
	# put path and name of db and xml files here 
	# string connect = "sqlite_file:badstripfromconstructiondb_noblob.db"    
	# #
	string timetype = "runnumber"    
	untracked string BlobStreamerName="DefaultBlobStreamingService"
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked int32 messageLevel=2
	}
	
	VPSet toPut={ { string record = "SiStripBadStripRcd"
		string tag ="mytest_bs"
	      }
	    }    
                  
    }
        


	
        


module prod =  SiStripBadStripFromConstructionDB {
					untracked bool   printDebug = true
	
				 	#Put here all bad Modules by hand
					untracked vuint32 ext_bad_detids = {}

					string IOVMode     = "Run"
					bool SinceAppendMode = true	
					string Record        = "SiStripBadStripRcd"
					bool doStoreOnDB = false #true

	
						
               }

       path p = { prod }
        
       module print = AsciiOutputModule {}
       endpath ep = { print }
}
